@page "/"
@inject NavigationManager Navigation

<PageTitle>// AUTO_PC</PageTitle>

<div style="padding: 2rem 0;">
    <h1>AUTO<span style="color: #ff2d7b; -webkit-text-fill-color: #ff2d7b;">PC</span></h1>
    <p style="font-family: 'Share Tech Mono', monospace; color: #5a6988; letter-spacing: 0.1em; margin-top: 0.5rem;">
        // REDIRECTING TO ASSISTANT...
    </p>
</div>

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            Navigation.NavigateTo("/chat", forceLoad: true);
        }
    }
}
